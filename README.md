# Trappy, ingilizce öğrenmenin eğlenceli hali !

## Server scripts

- `yarn start` for production
- `yarn serve` for development

## Client scripts

- `yarn lint` for debug and compile
- `yarn build` for project build
- `yarn serve` for development
